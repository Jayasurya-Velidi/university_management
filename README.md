# pip install streamlit

streamlit run main.py

python -m streamlit run main.py

# uploading the project on GITHUB
# step-1  creating a repository on github
# step-2  in gitbash change to your project directory : cd
# step-3  clone the repo

#  git clone URL_of_repository 

# step-4 copy the main.py & requirements.txt inside the local repo
# step-5 cd repo folder
# step-6  Run below commands in gitbash
git status
git add.
git commit-m "Project uploaded sucessfully"
git push